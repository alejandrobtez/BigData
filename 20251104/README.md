# Practica 13 - Programación Modular

**Fecha de entrega:** 3 de noviembre de 2025

## 💻 Entorno de Trabajo

Esta práctica se desarrollará utilizando **Python** como lenguaje de programación. El entorno principal implicará la creación de **scripts (`.py`)** y su organización en **paquetes** (directorios). A diferencia de prácticas anteriores, el foco no está en un solo Notebook, sino en la estructura de ficheros del proyecto.

## 📝 Descripción

Bienvenido a la Práctica 13. Esta sesión se centra en un concepto fundamental para proyectos complejos: la **programación modular**.

El objetivo principal es aprender a estructurar el código dividiéndolo en múltiples ficheros (módulos) y agruparlos en directorios (paquetes). Se trabajarán conceptos clave como:

* La creación de módulos (ficheros `.py`).
* La definición de paquetes (directorios que contienen un fichero `__init__.py`).
* La importación de funciones y variables entre módulos (usando `import` y `from`).

El propósito es aprender a construir proyectos de Python que sean mantenibles, escalables y reutilizables.
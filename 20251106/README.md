# Practica 14 - Distribución y casos practicos de paquetes externos

## 💻 Entorno de Trabajo

Esta práctica se desarrollará utilizando **Python** como lenguaje de programación. El entorno de trabajo será mixto:

1.  **Editor de código y Terminal:** Para crear la estructura de directorios, los ficheros (`.py`) y el `setup.py`.
2.  **Línea de Comandos (Terminal):** Para ejecutar los comandos de construcción (`setuptools`) e instalación (`pip`).
3.  **Jupyter Notebook (`.ipynb`):** Para probar e importar los paquetes una vez que hayan sido creados e instalados.

## 📝 Descripción

Bienvenido a la Práctica 14. Esta sesión se enfoca en el paso final del desarrollo de código reutilizable: la **distribución de paquetes en Python**.

El objetivo principal es aprender el proceso completo para empaquetar un proyecto, de modo que pueda ser fácilmente instalado y utilizado por otros (o por ti mismo en otros proyectos). Se trabajarán conceptos clave como:

* La creación de un fichero `setup.py` para definir los metadatos del paquete.
* El uso de `setuptools` para generar los ficheros de distribución.
* La instalación local de un paquete con `pip`.
* La importación y uso de un paquete personalizado.
* La estructuración de un proyecto que incluye subpaquetes.

El propósito es ser capaz de crear, distribuir e instalar un proyecto de Python de forma estandarizada.